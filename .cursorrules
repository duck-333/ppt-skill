# 象形英语 PPT 自动化 SOP
## 🚫 行为禁令 (Behavioral Restrictions)

1. **禁止修改模板**：严禁以任何理由（包括但不限于环境报错、占位符未找到、格式优化等）修改、覆盖、删除或重新生成 `master_template.pptx` 文件。
2. **禁止自动创建脚本**：严禁自动生成并运行如 `make_master_template.py` 这种试图重建模板的脚本。
3. **资产保护**：`master_template.pptx` 必须被视为“只读资产”，所有 PPT 的生成逻辑必须基于该文件的当前状态。
4. **报错处理**：如果脚本因模板问题报错，请直接提示用户手动检查占位符，严禁尝试“推倒重来”。

当你看到用户上传图片并要求生成 PPT 时，请严格按照以下流程全自动执行，无需用户再次确认或输入指令：

1. **OCR 文字提取阶段**：
   - 识别用户上传的所有图片。
   - 调用项目中的 `extract_text.py` 逻辑（或直接使用你的 Vision 能力）提取图片中的中英文内容。
   - 必须按照 [词根] -> [释义] -> [单词列表] 的结构整理。
   - 自动覆盖写入到项目目录下的 `content.txt` 文件。

2. **PPT 自动化生成阶段**：
   - 读取刚刚更新的 `content.txt`。
   - 必须使用目录下的 `master_template.pptx` 作为模板。
   - 自动运行 `ppt_maker.py` 脚本。
   - **严格遵守协议**：
     - Slide 1 (封面)：更新标题。
     - Slide 2 (详情)：按词根数量循环克隆，匹配 WORD_NAME, WORD_DEFINITION 等占位符。
     - Slide 3 (总结)：按词根数量动态克隆 SUMMARY_UNIT_TEMPLATE，并确保每个卡片数据独立。

3. **最终交付**：
   - 生成完成后，直接告知用户 PPT 的文件名和存放路径。
   - 提醒用户可以点击左侧文件栏查看。

## 4. 版式自适应补充协议（已固化）

以下规则必须作为 `ppt_maker.py` 的默认生成协议执行：

- **顶部角标统一**：所有页面左上角 `{{TOP_CATEGORY}}` 使用极短角标；字母课只写单个大写字母，例如 `A`、`B`、`C`，不带“字母”二字，也不带后面的“--足”“-树干”等释义内容。正文主标题和意项标题仍保留完整含义。
- **Word 文档优先**：当用户提供 `.docx` 时，优先从 Word 文档直接提取文字，避免 OCR 丢字、音标缺失或顺序错乱；输出 PPT 文件名必须与 Word 文档同名。
- **详情页主单词颜色**：`WORD_NAME` 必须使用纯红色 `#FF0000`。
- **详情页音标保留**：有音标时显示为 `/音标/`；无音标时不得出现 `//`。
- **详情页发音按钮**：默认在每个详情页 `WORD_NAME` 旁生成两个大号发音按钮，中文标为 `英` 与 `美`；按钮必须使用 PPT 动作声音（`hlinkClick + snd`）直接触发发音，放映时点击按钮本身就播放，不得变成需要二次点击播放键的媒体控件。
- **动作声音格式**：`英/美` 按钮触发的音频必须优先使用 PCM WAV；在线下载得到 MP3 后必须自动转成 WAV 再嵌入，避免 PowerPoint 对 MP3 动作声音支持不稳定导致点击无声。
- **发音音频来源**：优先联网下载英式/美式标准读音并嵌入 PPT；若联网不可用，则尝试调用本机 Windows SAPI 对应地区语音兜底。缺少某个口音语音时必须提示并跳过，不得用美式语音冒充英式。
- **发音缺失处理**：如果某个词语在网上和本机都找不到对应口音音频，应先汇总缺失词语并询问用户是否跳过或补充音频；用户确认后再继续交付，不要静默遗漏。
- **发音按钮开关**：默认启用发音按钮；如需临时关闭，可运行 `ppt_maker.py --no-audio`。
- **详情页单词意思字号**：`WORD_DEFINITION` 优先保持 36 号；如内容太长需要自适应，最低不得小于 30 号。
- **详情页单词意思颜色**：`WORD_DEFINITION` 必须使用纯红色 `#FF0000`，与主单词强调色一致。
- **详情页解释文本自适应**：`WORD_ANALYSIS` 必须启用自动换行，文本不得超出白色卡片；长解释优先在卡片内换行和扩展高度，再适度缩小字号。
- **详情页释义顺序**：白色卡片内 `WORD_ANALYSIS`（长段构词/释义）必须放在 `WORD_DEFINITION`（中文词义）上方；中文词义放在长段解释下方，仍遵守 36 号优先、最低 30 号规则。
- **释义行归并**：Word 中以 `【释义】` 开头的段落应合并到上一条真实词条的解释区，不得被误识别成新的无音标单词页。
- **词性解析完整**：除 `n.`、`v.`、`adj.`、`adv.` 外，也要正确支持 `prep.`、`conj.` 等词性，避免释义丢失。
- **意项编号识别**：必须识别 `①` 到 `⑨` 的意项标题；标题和解释挤在同一段时，只保留标题概念名。
- **总结页蓝色卡片自适应**：最后一页 `SUMMARY_UNIT_TEMPLATE` 克隆出的蓝色卡片必须根据意项数量自动调整宽度；单词列表必须根据文本长度自动换行、放大字号、调整卡片高度，文字不得掉出卡片。
- **总结页自动拆页**：Summary 自动拆成多页，每页最多 3 个蓝卡，确保长单词列表也有足够高度，所有卡片都在页面范围内。
- **总结页字号策略**：总结卡片单词列表应尽量放大，优先使用 28 号；内容太长时逐级缩小，但必须保证仍在卡片内部。
